VITE_API_URL=http://localhost:3001

# Cognito values are hardcoded in src/lib/auth.ts since they are public
# (User Pool IDs and public App Client IDs are safe to embed in the SPA bundle).
# Reference only:
#   COGNITO_USER_POOL_ID = us-east-1_WucFi2sNK
#   COGNITO_CLIENT_ID    = 3i724l9g6bb4qffde95n7u5sgm
