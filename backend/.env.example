PORT=3001
AWS_REGION=us-east-1
# Local AWS named profile that points at account 381753457015
AWS_PROFILE=openclaw
TABLE_NAME=gym-workout-engine-prod
STAGE=prod
FRONTEND_URL=http://localhost:5173

# Cognito (used by JWT-validation middleware; public values, not secrets)
COGNITO_REGION=us-east-1
COGNITO_CLIENT_ID=3i724l9g6bb4qffde95n7u5sgm
COGNITO_USER_POOL_ID=us-east-1_WucFi2sNK

# Secrets Manager (open-claw-secrets JSON fields — never put real values here)
#   gym-workout-engine-api-key  — Anthropic / Claude
#   muscle-wiki-api-key         — MuscleWiki exercise videos

# Local-dev only: set this in backend/.env (gitignored) to skip the Secrets
# Manager lookup when the local IAM user lacks secretsmanager:GetSecretValue.
# Leave unset in Lambda — prod always reads from open-claw-secrets.
# MUSCLE_WIKI_API_KEY=mw_xxxxxxxxxxxxx
